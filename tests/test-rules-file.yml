name: Test ZAP Full Scan - Rules File
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          target: 'https://www.example.com'
          rules_file_name: 'actions/hardened/zaproxy--action-full-scan/v0.11.0/tests/fixtures/zap-rules.tsv'
          fail_action: false
          allow_issue_writing: false
        continue-on-error: true
      - name: Assert action ran with rules_file_name
        run: |
          echo "ZAP Full Scan action completed with rules_file_name input"
          echo "PASS: rules_file_name input was accepted and action executed"
